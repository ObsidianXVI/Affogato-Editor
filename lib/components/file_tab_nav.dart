part of affogato.components;

class FileTabNavComponent extends StatelessWidget {
  const FileTabNavComponent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 50,
    );
  }
}
