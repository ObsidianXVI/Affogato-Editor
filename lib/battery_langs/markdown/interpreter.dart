part of affogato.editor.battery.langs.markdown;
class MarkdownInterpreter extends Interpreter {
  @override
  void interpret(AST ast) {
    throw Exception("Markdown does not have interpreter support.");
  }
}
